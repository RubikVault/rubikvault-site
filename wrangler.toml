name = "rubikvault-site"
compatibility_date = "2025-12-29"

# Cloudflare Pages Functions KV bindings are configured in the Dashboard
# (Preview + Production). This file only tracks build/dev metadata.
